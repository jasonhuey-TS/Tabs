# Base URL for the FastAPI backend
# In dev this is handled by Vite's proxy above, so you usually don't need to set this.
# Set it explicitly for staging/prod builds.
VITE_API_BASE_URL=http://localhost:8000
